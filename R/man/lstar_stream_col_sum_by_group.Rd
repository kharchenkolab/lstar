% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels.R
\name{lstar_stream_col_sum_by_group}
\alias{lstar_stream_col_sum_by_group}
\title{Per-(group, gene) sums of a CSC measure in a store, streamed and bounded-memory.}
\usage{
lstar_stream_col_sum_by_group(
  path,
  field,
  group,
  ngroups,
  lognorm = FALSE,
  depth = NULL,
  depthScale = 1,
  block = 4096L,
  n_threads = 1L
)
}
\arguments{
\item{path}{path to a \verb{*.lstar.zarr} store.}

\item{field}{name of a \verb{(cells, genes)} CSC measure field in the store.}

\item{group}{integer per-cell group bucket in \verb{[0, ngroups)}, in store row order.}

\item{ngroups}{number of groups (the result has one row per group).}

\item{lognorm}{compute over the \code{log1p} view of the measure (default \code{FALSE}).}

\item{depth}{optional per-cell depth vector (length = n cells, store row order) for the normalized view.}

\item{depthScale}{depth scaling factor used with \code{depth} (default 1).}

\item{block}{streaming block size in cells per pass (default 4096).}

\item{n_threads}{threads per block reduction: 1 = serial (default), N = N threads, 0 = all cores.}
}
\value{
a \verb{ngroups x ngenes} numeric matrix (row \code{g} = the per-gene sums for group \code{g}).
}
\description{
Streaming, fused pseudobulk: per cell-group sums of each gene, computed in one threaded pass over a
chunked CSC store, with the same optional depth-normalized \code{log1p} view as \code{\link[=stream_col_stats]{stream_col_stats()}} (the
counterpart of pagoda2's \code{colSumByFacView}). \code{group} is a per-cell integer bucket in \verb{[0, ngroups)}
in store row order (out-of-range cells are skipped; pass NA cells as \code{0} for an explicit \verb{<NA>} row).
}
