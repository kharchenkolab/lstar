% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collection.R
\name{collection_from}
\alias{collection_from}
\title{Assemble a collection of heterogeneous samples from per-sample objects.}
\usage{
collection_from(
  samples,
  joint = NULL,
  sample_field = "sample",
  prefix_cells = TRUE
)
}
\arguments{
\item{samples}{a named list of per-sample \code{lstar_dataset}s (or \code{Seurat} / \code{SingleCellExperiment}
objects, read via the profiles). Each must have \code{cells} and \code{genes} axes.}

\item{joint}{optional named list of fields over the \strong{union} cells (the integration outputs): a matrix
becomes a joint embedding; a factor/character vector a clustering (inducing a factor axis); a
\verb{(cells x cells)} sparse matrix a graph relation.}

\item{sample_field}{name of the design label recording each union cell's sample (default \code{"sample"}).}

\item{prefix_cells}{prefix each cell label with its sample name so union labels are unique (default \code{TRUE}).}
}
\value{
an \code{lstar_dataset} of kind \code{"collection"}.
}
\description{
A \emph{collection} keeps each sample's own data --- per-sample \verb{cells.<s>}/\verb{genes.<s>} axes and
\verb{<field>.<s>} fields, over gene sets that may overlap, differ, or be entirely \strong{disjoint} across
samples --- alongside a \code{samples} axis and a \emph{union} \code{cells} axis carrying the joint analysis (a shared
embedding, clusters, a graph). This is lstar's "a collection is not one aligned tensor" model, built
from any list of separately-processed samples rather than hand-assembled.
}
\examples{
mk <- function(s, nc, genes) {
  ds <- list(kind = "sample", axes = list(), fields = list())
  ds$axes$cells <- list(labels = paste0("c", seq_len(nc)), origin = "observed", role = "observation")
  ds$axes$genes <- list(labels = genes, origin = "observed", role = "feature")
  m <- as(Matrix::Matrix(matrix(rpois(nc * length(genes), 2), nc), sparse = TRUE), "CsparseMatrix")
  ds$fields$counts <- list(role = "measure", span = c("cells", "genes"), state = "raw", values = m)
  class(ds) <- "lstar_dataset"; ds
}
col <- collection_from(list(A = mk("A", 5, paste0("g", 1:8)),
                            B = mk("B", 7, paste0("g", 3:12))))   # divergent gene sets
col$kind
}
\seealso{
\code{\link[=write_conos]{write_conos()}}, \code{\link[=read_seurat]{read_seurat()}}
}
