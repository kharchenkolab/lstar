% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile_pagoda2.R
\name{read_pagoda2}
\alias{read_pagoda2}
\title{Read a Pagoda2 object into an L* dataset.}
\usage{
read_pagoda2(p2)
}
\arguments{
\item{p2}{a Pagoda2 (pagoda2.1) object (or a \code{getRawCounts()}/\code{embeddings}/\code{cellMeta}-shaped object).}
}
\value{
an \code{lstar_dataset} (kind \code{"sample"}).
}
\description{
The pagoda2 counterpart of \code{\link[=read_seurat]{read_seurat()}}: extracts \strong{every facet} (RNA/ADT/ATAC… raw counts over
their feature axes), \strong{all embeddings} (\code{embeddings[[reduction]][[name]]}), and \strong{all \code{cellMeta}
columns} (categorical → a \code{label} field inducing a factor axis; numeric → a \code{measure}), each carrying
the provenance (\code{pagoda2::Pagoda2$fromLstar()} uses it to reconstruct the object).
Duck-typed: a facet-aware pagoda2.1 object uses \code{listFacets()}/\code{getFacet()}; a simpler object falls
back to \code{getRawCounts()} (single RNA facet). Viewer navigators are NOT added here — call
\code{\link[=extend_for_viewer]{extend_for_viewer()}} for that.
}
\seealso{
\code{\link[=extend_for_viewer]{extend_for_viewer()}}, \code{\link[=read_seurat]{read_seurat()}}
}
