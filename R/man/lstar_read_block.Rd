% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels.R
\name{lstar_read_block}
\alias{lstar_read_block}
\title{Read a contiguous gene (column) range of a CSC measure from an L* store, bounded-memory.}
\usage{
lstar_read_block(path, field, g_lo, g_hi, cell_names = NULL, gene_names = NULL)
}
\arguments{
\item{path}{path to a \verb{*.lstar.zarr} store.}

\item{field}{name of a \verb{(cells, genes)} CSC measure field in the store.}

\item{g_lo, g_hi}{0-based, half-open gene (column) range \verb{[g_lo, g_hi)} to read.}

\item{cell_names, gene_names}{optional dimnames for the returned matrix (default \code{NULL}).}
}
\value{
a \code{dgCMatrix} (cells x genes) holding the requested gene columns.
}
\description{
Reads genes \verb{[g_lo, g_hi)} (0-based, half-open) of a CSC measure as a \code{dgCMatrix} (cells x genes),
decoding only the store chunks that overlap the range. The general block-read primitive a consumer
drives to build out-of-core reductions over an L* store without implementing them in lstar.
}
