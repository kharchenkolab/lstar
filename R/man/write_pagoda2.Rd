% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile_pagoda2.R
\name{write_pagoda2}
\alias{write_pagoda2}
\title{Export a Pagoda2 object to an L* (\verb{*.lstar.zarr}) store.}
\usage{
write_pagoda2(p2, path = NULL, grouping = "leiden")
}
\arguments{
\item{p2}{a Pagoda2 (pagoda2.1) object.}

\item{path}{output store path (\verb{*.lstar.zarr}); if \code{NULL}, only the \code{lstar_dataset} is returned.}

\item{grouping}{a \code{cellMeta} column to use as the primary clustering (default \code{"leiden"}).}
}
\value{
an \code{lstar_dataset} (invisibly if written).
}
\description{
Writes counts (raw, cell x gene), the embedding, cluster/cell-type/QC labels, and the
viewer profile's cluster sufficient stats + marker tables (\verb{viewer@0.1}). Computes the
cluster stats with the shared libstar kernel.
}
