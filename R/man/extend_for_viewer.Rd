% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viewer.R
\name{extend_for_viewer}
\alias{extend_for_viewer}
\title{Extend an L* dataset with the viewer@0.1 navigator fields (native R).}
\usage{
extend_for_viewer(
  ds,
  grouping = NULL,
  also = character(0),
  counts = NULL,
  basis = NULL,
  order = "hybrid",
  markers = TRUE,
  primary = NULL
)
}
\arguments{
\item{ds}{an \code{lstar_dataset} (a raw-counts measure + at least one categorical cell label).}

\item{grouping}{primary grouping label (default: auto-detect; clustering/cell-type names preferred).}

\item{also}{additional grouping labels to also summarize (e.g. \code{"cell_type"}).}

\item{counts}{force the count measure to build from (\code{log1p} unless it is already log-normalized).
\code{NULL} (default) lets \code{basis} choose.}

\item{basis}{how to choose the measure by state (\code{NULL} == \code{"auto"}): \code{"auto"} prefers a raw measure
(\code{log1p}-transformed) and, when none is present, falls back to a log-normalized one (used as-is;
stats are var-of-lognorm, so HVG/markers are approximate -- a warning is emitted). \code{"raw"}/
\code{"lognorm"} force that state. A scaled/z-scored measure is never used.}

\item{order}{\code{"hybrid"} (default) physically reorders \code{counts_cellmajor} (cluster-contiguous, then a
Hilbert curve over the embedding) and adds \code{counts_cellmajor_order}; \code{"none"} keeps rows in cell
order and omits the permutation.}

\item{markers}{if \code{TRUE} (default), also compute the 1-vs-rest \verb{markers_<g>_\{lfc,padj\}} tables.}

\item{primary}{the grouping the \emph{viewer opens on}. Hoisted to the front of the prepared groupings, so it
keys the \code{counts_cellmajor} locality reorder AND is summarized first -- the eager-prepare a fast launch
waits on. Unlike ordering the groupings by hand, \code{primary} composes with auto-detect: \code{primary="cell_type"}
with \code{grouping=NULL} preps \emph{every} detected grouping but keys the reorder on \code{cell_type} (the auto-detect
policy prefers clusterings, but the viewer may open on a cell-type annotation). \code{NULL} = current behavior.}
}
\value{
\code{ds} with the navigator fields added and \verb{viewer@0.1} in \code{ds$profiles}.
}
\description{
Precomputes, via the shared libstar kernels: per-grouping cluster sufficient stats
(\verb{stats_<g>_\{sum,sumsq,nexpr\}}, group-major), 1-vs-rest marker tables (\verb{markers_<g>_\{lfc,padj\}},
gene-major), a whole-dataset \code{od_score} (pagoda2 lowess + F-test), a cell-major \code{counts_cellmajor}
physically reordered cluster-contiguous, and its \code{counts_cellmajor_order} permutation. Stamps the
\verb{viewer@0.1} profile. The store this writes is interchangeable with a Python/JS-prepped one.
}
\seealso{
\code{\link[=viewer_extend]{viewer_extend()}}, \code{\link[=lstar_write_viewer]{lstar_write_viewer()}}
}
