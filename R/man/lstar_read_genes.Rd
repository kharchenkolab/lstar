% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels.R
\name{lstar_read_genes}
\alias{lstar_read_genes}
\title{Read an arbitrary set of gene columns of a CSC measure, returning cells x genes.}
\usage{
lstar_read_genes(path, field, genes, all_genes, cell_names = NULL)
}
\arguments{
\item{path}{path to a \verb{*.lstar.zarr} store.}

\item{field}{name of a \verb{(cells, genes)} CSC measure field in the store.}

\item{genes}{the gene columns to gather, as names (matched against \code{all_genes}) or 1-based indices.}

\item{all_genes}{the field's full gene-label vector, used to resolve \code{genes} to column positions.}

\item{cell_names}{optional row names (cells) for the returned matrix (default \code{NULL}).}
}
\value{
a \code{dgCMatrix} (cells x \code{length(genes)}) in the caller's requested gene order.
}
\description{
Gathers the requested gene columns from a chunked CSC store, decoding each touched chunk \strong{at most
once} (an ascending sweep over sorted-unique columns), then restores the caller's order. Efficient
for scattered subsets (e.g. overdispersed genes for PCA) -- unlike a per-column read it does not
re-decode a chunk once per gene it contains.
}
