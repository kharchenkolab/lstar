% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de.R
\name{lstar_markers}
\alias{lstar_markers}
\title{Tidy marker table for a factor's DE bundle.}
\usage{
lstar_markers(ds, factor, top = NULL, sort_by = "score", descending = TRUE)
}
\arguments{
\item{ds}{an \code{lstar_dataset} (e.g. from \code{\link[=lstar_read]{lstar_read()}}).}

\item{factor}{the factor-axis name the DE was computed over (e.g. \code{"leiden"}).}

\item{top}{optional: keep only the top-N genes per group (by \code{sort_by}).}

\item{sort_by}{statistic to rank within a group (default \code{"score"}).}

\item{descending}{sort descending (default \code{TRUE}).}
}
\value{
a data frame with columns \code{group}, \code{gene}, and the available statistics.
}
\description{
Gathers the \verb{de.<factor>.<stat>} fields (score/lfc/pval/padj over the factor x genes axes) into a
long-form data frame: one row per (group, gene) with whichever statistics are present.
}
