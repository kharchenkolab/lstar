% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backed.R
\name{read_seurat_backed}
\alias{read_seurat_backed}
\title{Open an .h5ad's expression matrix as a disk-backed Seurat v5 assay (via BPCells).}
\usage{
read_seurat_backed(h5ad, group = "X", assay = "RNA", project = "lstar")
}
\arguments{
\item{h5ad}{path to an .h5ad file (its \code{X}/layer stays on disk).}

\item{group}{h5ad group to read as the matrix (default \code{"X"}; e.g. \code{"layers/counts"}).}

\item{assay}{name for the Seurat assay (default \code{"RNA"}).}

\item{project}{Seurat project name.}
}
\value{
a Seurat object whose assay counts are a disk-backed BPCells matrix.
}
\description{
Reads the matrix from \code{h5ad} with BPCells (\code{open_matrix_anndata_hdf5}) so it stays on disk as a
streaming \code{IterableMatrix} -- already oriented genes x cells (rownames genes, colnames cells), the
Seurat convention -- and wraps it in a Seurat v5 \code{Assay5}. The matrix is never materialized: peak
memory is a few megabytes regardless of atlas size. Typical use is the bounded end of an L*
conversion, after \code{lstar.convert_to_h5ad(store, h5ad)} in Python:
}
\details{
\preformatted{
  so <- read_seurat_backed("atlas.h5ad")          # counts live on disk (BPCells)
  so <- Seurat::NormalizeData(so)                  # Seurat v5 ops stream off disk
}
}
