% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels.R
\name{stream_col_stats}
\alias{stream_col_stats}
\title{Per-gene mean/variance of a CSC measure in a store, read with bounded memory.}
\usage{
stream_col_stats(
  path,
  field,
  block = 4096L,
  n_threads = 1L,
  lognorm = FALSE,
  depth = NULL,
  depthScale = 1,
  population = FALSE
)
}
\arguments{
\item{path}{path to an L\* store (\code{.lstar.zarr}).}

\item{field}{measure field name (e.g. \code{"counts"}), stored CSC.}

\item{block}{number of gene columns per streamed block (default 4096).}

\item{n_threads}{threading policy for each block's reduction: 1 = serial (default), N = N threads,
\verb{<=0} = all cores. Results are thread-count invariant.}

\item{lognorm}{reduce over \code{log1p(x)} per nonzero, on the fly, without building the normalized
matrix (default \code{FALSE}).}

\item{depth}{optional per-cell depth vector (length n cells, in store row order). When given, each
nonzero is normalized to \code{x * depthScale / depth[cell]} before the optional \code{log1p} -- i.e.
pagoda2's "plain" analysis view, computed in one streamed pass (the block's row \code{indices} are read
too). \code{NULL} (default) reduces the raw/log1p values.}

\item{depthScale}{depth scaling factor (default 1) used with \code{depth}.}

\item{population}{if \code{TRUE}, variance divides by \code{n} (population, pagoda2's convention) instead of
the sample \code{n-1} (default \code{FALSE}).}
}
\value{
a list with \code{mean}, \code{var} (length \code{ngenes} numeric) and \code{nnz} (length \code{ngenes} integer).
}
\description{
Computes the zero-aware per-gene mean, variance, and number of expressing cells of a \verb{cells x genes} measure directly from an L\* store, reading it \strong{block-by-block} so the whole matrix never
lands in memory -- the C++/R counterpart of Python's \code{stream_col_stats}. Bounded memory requires a
chunked store (one written with \code{chunk_elems} set, e.g. by a streamed conversion); on an unchunked
store it still works but reads the \code{data} array whole. The field must be CSC (gene-major); use it
for HVG selection / variance modeling over an atlas too large to load.
}
